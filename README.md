#ConvertImagetoCahracterEncodingImage

Convert image file to a new image file with character encoded, support commons image file type, such as `.jpg`, `jpeg`, `.bmp`, `.png`, but also support `.gif` image file.
